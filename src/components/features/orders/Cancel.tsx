function Cancel({ orders }: any) {
  return (
    <div>Cancel
      {orders.map(() => (
        // <div>
        //   {item.name}
        // </div>
        <></>
      ))}
    </div>
  )
}

export default Cancel